# This is a config file for the crypto notifier

# Coin's / token's symbol
[symbol]
symbol = BTC

# Amount of your coins to be calculated with price | "0" if you don't need it
[amount]
amount = 0

# Coinmarketcap API token
[token]
cmc_token = your_token_here